<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <!-- Default portal config — password will be set via UI (hashed) -->
    <record id="health_portal_config_default" model="health.portal.config">
        <field name="name">Portal Configuration</field>
    </record>
</odoo>
